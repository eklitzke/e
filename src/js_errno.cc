// Copyright 2012, Evan Klitzke <evan@eklitzke.org>

#include "./js.h"
#include "./js_errno.h"

namespace e {
namespace js_errno {
bool Build(v8::Handle<v8::Object> obj) {
  v8::HandleScope scope;
  NEW_INTEGER(obj, EPERM);
  NEW_INTEGER(obj, ENOENT);
  NEW_INTEGER(obj, ESRCH);
  NEW_INTEGER(obj, EINTR);
  NEW_INTEGER(obj, EIO);
  NEW_INTEGER(obj, ENXIO);
  NEW_INTEGER(obj, E2BIG);
  NEW_INTEGER(obj, ENOEXEC);
  NEW_INTEGER(obj, EBADF);
  NEW_INTEGER(obj, ECHILD);
  NEW_INTEGER(obj, EAGAIN);
  NEW_INTEGER(obj, ENOMEM);
  NEW_INTEGER(obj, EACCES);
  NEW_INTEGER(obj, EFAULT);
  NEW_INTEGER(obj, ENOTBLK);
  NEW_INTEGER(obj, EBUSY);
  NEW_INTEGER(obj, EEXIST);
  NEW_INTEGER(obj, EXDEV);
  NEW_INTEGER(obj, ENODEV);
  NEW_INTEGER(obj, ENOTDIR);
  NEW_INTEGER(obj, EISDIR);
  NEW_INTEGER(obj, EINVAL);
  NEW_INTEGER(obj, ENFILE);
  NEW_INTEGER(obj, EMFILE);
  NEW_INTEGER(obj, ENOTTY);
  NEW_INTEGER(obj, ETXTBSY);
  NEW_INTEGER(obj, EFBIG);
  NEW_INTEGER(obj, ENOSPC);
  NEW_INTEGER(obj, ESPIPE);
  NEW_INTEGER(obj, EROFS);
  NEW_INTEGER(obj, EMLINK);
  NEW_INTEGER(obj, EPIPE);
  NEW_INTEGER(obj, EDOM);
  NEW_INTEGER(obj, ERANGE);
  NEW_INTEGER(obj, EDEADLOCK);
  NEW_INTEGER(obj, ENAMETOOLONG);
  NEW_INTEGER(obj, ENOLCK);
  NEW_INTEGER(obj, ENOSYS);
  NEW_INTEGER(obj, ENOTEMPTY);
  NEW_INTEGER(obj, ELOOP);
  NEW_INTEGER(obj, ENOMSG);
  NEW_INTEGER(obj, EIDRM);
  NEW_INTEGER(obj, ECHRNG);
  NEW_INTEGER(obj, EL2NSYNC);
  NEW_INTEGER(obj, EL3HLT);
  NEW_INTEGER(obj, EL3RST);
  NEW_INTEGER(obj, ELNRNG);
  NEW_INTEGER(obj, EUNATCH);
  NEW_INTEGER(obj, ENOCSI);
  NEW_INTEGER(obj, EL2HLT);
  NEW_INTEGER(obj, EBADE);
  NEW_INTEGER(obj, EBADR);
  NEW_INTEGER(obj, EXFULL);
  NEW_INTEGER(obj, ENOANO);
  NEW_INTEGER(obj, EBADRQC);
  NEW_INTEGER(obj, EBADSLT);
  NEW_INTEGER(obj, EBFONT);
  NEW_INTEGER(obj, ENOSTR);
  NEW_INTEGER(obj, ENODATA);
  NEW_INTEGER(obj, ETIME);
  NEW_INTEGER(obj, ENOSR);
  NEW_INTEGER(obj, ENONET);
  NEW_INTEGER(obj, ENOPKG);
  NEW_INTEGER(obj, EREMOTE);
  NEW_INTEGER(obj, ENOLINK);
  NEW_INTEGER(obj, EADV);
  NEW_INTEGER(obj, ESRMNT);
  NEW_INTEGER(obj, ECOMM);
  NEW_INTEGER(obj, EPROTO);
  NEW_INTEGER(obj, EMULTIHOP);
  NEW_INTEGER(obj, EDOTDOT);
  NEW_INTEGER(obj, EBADMSG);
  NEW_INTEGER(obj, EOVERFLOW);
  NEW_INTEGER(obj, ENOTUNIQ);
  NEW_INTEGER(obj, EBADFD);
  NEW_INTEGER(obj, EREMCHG);
  NEW_INTEGER(obj, ELIBACC);
  NEW_INTEGER(obj, ELIBBAD);
  NEW_INTEGER(obj, ELIBSCN);
  NEW_INTEGER(obj, ELIBMAX);
  NEW_INTEGER(obj, ELIBEXEC);
  NEW_INTEGER(obj, EILSEQ);
  NEW_INTEGER(obj, ERESTART);
  NEW_INTEGER(obj, ESTRPIPE);
  NEW_INTEGER(obj, EUSERS);
  NEW_INTEGER(obj, ENOTSOCK);
  NEW_INTEGER(obj, EDESTADDRREQ);
  NEW_INTEGER(obj, EMSGSIZE);
  NEW_INTEGER(obj, EPROTOTYPE);
  NEW_INTEGER(obj, ENOPROTOOPT);
  NEW_INTEGER(obj, EPROTONOSUPPORT);
  NEW_INTEGER(obj, ESOCKTNOSUPPORT);
  NEW_INTEGER(obj, EOPNOTSUPP);
  NEW_INTEGER(obj, EPFNOSUPPORT);
  NEW_INTEGER(obj, EAFNOSUPPORT);
  NEW_INTEGER(obj, EADDRINUSE);
  NEW_INTEGER(obj, EADDRNOTAVAIL);
  NEW_INTEGER(obj, ENETDOWN);
  NEW_INTEGER(obj, ENETUNREACH);
  NEW_INTEGER(obj, ENETRESET);
  NEW_INTEGER(obj, ECONNABORTED);
  NEW_INTEGER(obj, ECONNRESET);
  NEW_INTEGER(obj, ENOBUFS);
  NEW_INTEGER(obj, EISCONN);
  NEW_INTEGER(obj, ENOTCONN);
  NEW_INTEGER(obj, ESHUTDOWN);
  NEW_INTEGER(obj, ETOOMANYREFS);
  NEW_INTEGER(obj, ETIMEDOUT);
  NEW_INTEGER(obj, ECONNREFUSED);
  NEW_INTEGER(obj, EHOSTDOWN);
  NEW_INTEGER(obj, EHOSTUNREACH);
  NEW_INTEGER(obj, EALREADY);
  NEW_INTEGER(obj, EINPROGRESS);
  NEW_INTEGER(obj, ESTALE);
  NEW_INTEGER(obj, EUCLEAN);
  NEW_INTEGER(obj, ENOTNAM);
  NEW_INTEGER(obj, ENAVAIL);
  NEW_INTEGER(obj, EISNAM);
  NEW_INTEGER(obj, EREMOTEIO);
  NEW_INTEGER(obj, EDQUOT);
  return true;
}
}
}
